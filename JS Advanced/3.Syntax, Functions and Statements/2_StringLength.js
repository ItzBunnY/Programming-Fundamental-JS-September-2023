function solve(str1, str2, str3){
    let totalLength = str1.length;
    totalLength += str2.length;
    totalLength += str3.length;

    console.log(totalLength);
    console.log(Math.floor(totalLength/3));
}
